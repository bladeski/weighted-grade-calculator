template#CalculatorItemTemplate
  link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=delete")
  div
    label
      span Assessment/Exam name
      input(type="text" placeholder="e.g. Midterm Exam")
    label
      span Weight (%)
      input(type="number", min="1", max="100" placeholder="e.g. 20" required)
    label
      span Mark (%)
      input(type="number", min="0", max="100" placeholder="e.g. 85" required)
    button(type="button", class="remove-item")
      span.material-symbols-outlined delete
